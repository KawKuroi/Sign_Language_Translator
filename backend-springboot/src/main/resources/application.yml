spring:
  application:
    name: sign-language-backend
  datasource:
    url: jdbc:postgresql://localhost:5432/signlangdb
    username: postgres
    password: password
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
server:
  port: 8080
